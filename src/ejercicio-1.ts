/**
 * ```typescript
 *  console.log(muestraTexto("Hola mundo"));
 * ```
 * _Función_ que devuelve un __texto de entrada__
 * @param texto Texto de entrada
 * @return Devuelve el texto
 */
function muestraTexto(texto: string): string {
  return texto;
}

console.log(muestraTexto("Hola mundo"));
